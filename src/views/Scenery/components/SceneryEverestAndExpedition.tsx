import React from "react"
import { Flex } from "uikit"

const SceneryEverestAndExpedition: React.FC = () => {
    return (
        <Flex width='100%' alignItems='center' justifyContent='space-around'>
            EVEREST AND EXPEDITION
        </Flex>
    )
}

export default React.memo(SceneryEverestAndExpedition)